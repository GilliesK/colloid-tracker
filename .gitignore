# Recordings / captured data (hundreds of MB, regenerated by the microscope)
*.mp4
*_lossless/
*.tif
*.tiff
buckling_out/

# Build artifacts
build/
dist/
build_venv/
__pycache__/
*.pyc
*.spec.bak

# Runtime state (rewritten on every app close — noisy diffs)
colloid_settings.json
crash_log.txt*

# Machine-local Claude session state (shared .claude/settings.json IS tracked)
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Stray/temp
%TEMP%/
*.bak
